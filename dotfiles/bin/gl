#!/bin/bash

source ~/bin/git-params.sh

if [[ ${VERBOSE-0} == 1 ]]; then
2>&1 echo "opts   ${opts[@]}"
2>&1 echo "params ${params[@]}"
2>&1 echo "files  ${files[@]}"
fi

git log --stat ${opts[@]} ${params[@]} -- ${files[@]}
